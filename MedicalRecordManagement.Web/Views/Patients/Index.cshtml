@model IEnumerable<MedicalRecordManagement.Core.Entities.Patient>

@{
    ViewData["Title"] = "Danh sách Bệnh nhân";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>👥 Danh sách Bệnh nhân</h2>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus"></i> Thêm bệnh nhân mới
    </a>
</div>

<div class="card">
    <div class="card-body">
        @if (Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Mã BN</th>
                            <th>Họ tên</th>
                            <th>Ngày sinh</th>
                            <th>Giới tính</th>
                            <th>Số điện thoại</th>
                            <th>Email</th>
                            <th>Thao tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <span class="badge bg-primary">@Html.DisplayFor(modelItem => item.PatientCode)</span>
                                </td>
                                <td>
                                    <strong>@Html.DisplayFor(modelItem => item.FullName)</strong>
                                </td>
                                <td>@item.DateOfBirth.ToString("dd/MM/yyyy")</td>
                                <td>
                                    <span class="badge bg-info">@Html.DisplayFor(modelItem => item.Gender)</span>
                                </td>
                                <td>@Html.DisplayFor(modelItem => item.PhoneNumber)</td>
                                <td>@Html.DisplayFor(modelItem => item.Email)</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">
                                            <i class="fas fa-eye"></i> Xem
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-warning">
                                            <i class="fas fa-edit"></i> Sửa
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">
                                            <i class="fas fa-trash"></i> Xóa
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-5">
                <i class="fas fa-users fa-3x text-muted mb-3"></i>
                <h4 class="text-muted">Chưa có bệnh nhân nào</h4>
                <p class="text-muted">Hãy thêm bệnh nhân đầu tiên để bắt đầu sử dụng hệ thống.</p>
                <a asp-action="Create" class="btn btn-primary">
                    <i class="fas fa-plus"></i> Thêm bệnh nhân đầu tiên
                </a>
            </div>
        }
    </div>
</div>
