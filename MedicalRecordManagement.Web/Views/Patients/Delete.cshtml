@model MedicalRecordManagement.Core.Entities.Patient

@{
    ViewData["Title"] = "Xóa Bệnh nhân";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>👥 Xóa Bệnh nhân</h2>
    <a asp-action="Index" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i> Quay lại danh sách
    </a>
</div>

<div class="alert alert-warning" role="alert">
    <h4 class="alert-heading">⚠️ Cảnh báo!</h4>
    <p>Bạn có chắc chắn muốn xóa bệnh nhân này không? Hành động này không thể hoàn tác.</p>
    <hr>
    <p class="mb-0">Tất cả dữ liệu liên quan đến bệnh nhân này sẽ bị ảnh hưởng.</p>
</div>

<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h5>Thông tin bệnh nhân sẽ bị xóa</h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Mã bệnh nhân:</label>
                            <p class="form-control-plaintext">
                                <span class="badge bg-primary fs-6">@Html.DisplayFor(model => model.PatientCode)</span>
                            </p>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Họ và tên:</label>
                            <p class="form-control-plaintext fs-5">@Html.DisplayFor(model => model.FullName)</p>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Ngày sinh:</label>
                            <p class="form-control-plaintext">@Model.DateOfBirth.ToString("dd/MM/yyyy")</p>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Tuổi:</label>
                            <p class="form-control-plaintext">@Model.Age tuổi</p>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Giới tính:</label>
                            <p class="form-control-plaintext">
                                <span class="badge bg-info fs-6">@Html.DisplayFor(model => model.Gender)</span>
                            </p>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Số CMND/CCCD:</label>
                            <p class="form-control-plaintext">@Html.DisplayFor(model => model.NationalId)</p>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Số điện thoại:</label>
                            <p class="form-control-plaintext">@Html.DisplayFor(model => model.PhoneNumber)</p>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Email:</label>
                            <p class="form-control-plaintext">@Html.DisplayFor(model => model.Email)</p>
                        </div>
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Địa chỉ:</label>
                    <p class="form-control-plaintext">@Html.DisplayFor(model => model.Address)</p>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Tiền sử bệnh:</label>
                    <p class="form-control-plaintext">@Html.DisplayFor(model => model.MedicalHistory)</p>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Dị ứng:</label>
                    <p class="form-control-plaintext">@Html.DisplayFor(model => model.Allergies)</p>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-header">
                <h6>⚠️ Tác động khi xóa</h6>
            </div>
            <div class="card-body">
                <ul class="list-unstyled">
                    <li><i class="fas fa-exclamation-triangle text-warning"></i> Tất cả bệnh án liên quan</li>
                    <li><i class="fas fa-exclamation-triangle text-warning"></i> Tất cả lịch hẹn liên quan</li>
                    <li><i class="fas fa-exclamation-triangle text-warning"></i> Thông tin bệnh nhân sẽ bị xóa vĩnh viễn</li>
                    <li><i class="fas fa-exclamation-triangle text-warning"></i> Không thể khôi phục dữ liệu</li>
                </ul>
            </div>
        </div>

        <div class="card mt-3">
            <div class="card-header">
                <h6>💡 Gợi ý</h6>
            </div>
            <div class="card-body">
                <p class="text-muted">
                    Thay vì xóa, bạn có thể chỉnh sửa thông tin bệnh nhân để cập nhật dữ liệu mới.
                </p>
                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning btn-sm">
                    <i class="fas fa-edit"></i> Chỉnh sửa thay vì xóa
                </a>
            </div>
        </div>
    </div>
</div>

<div class="d-flex justify-content-end mt-4">
    <form asp-action="Delete" class="me-2">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">
            <i class="fas fa-trash"></i> Xác nhận xóa
        </button>
    </form>
    <a asp-action="Index" class="btn btn-secondary">
        <i class="fas fa-times"></i> Hủy
    </a>
</div>
