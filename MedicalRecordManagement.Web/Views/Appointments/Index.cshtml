@model IEnumerable<MedicalRecordManagement.Core.Entities.Appointment>

@{
    ViewData["Title"] = "Danh sách Lịch hẹn";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>📅 Danh sách Lịch hẹn</h2>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus"></i> Đặt lịch hẹn mới
    </a>
</div>

<div class="card">
    <div class="card-body">
        @if (Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Mã LH</th>
                            <th>Bệnh nhân</th>
                            <th>Bác sĩ</th>
                            <th>Ngày hẹn</th>
                            <th>Giờ hẹn</th>
                            <th>Lý do</th>
                            <th>Trạng thái</th>
                            <th>Thao tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <span class="badge bg-primary">@Html.DisplayFor(modelItem => item.AppointmentNumber)</span>
                                </td>
                                <td>
                                    <strong>@Html.DisplayFor(modelItem => item.Patient.FullName)</strong>
                                    <br>
                                    <small class="text-muted">@Html.DisplayFor(modelItem => item.Patient.PatientCode)</small>
                                </td>
                                <td>
                                    <strong>@Html.DisplayFor(modelItem => item.Doctor.FullName)</strong>
                                    <br>
                                    <small class="text-muted">@Html.DisplayFor(modelItem => item.Doctor.Specialization)</small>
                                </td>
                                <td>@item.AppointmentDate.ToString("dd/MM/yyyy")</td>
                                <td>@item.AppointmentTime.ToString(@"hh\:mm")</td>
                                <td>
                                    @if (!string.IsNullOrEmpty(item.Reason))
                                    {
                                        <span class="text-truncate d-inline-block" style="max-width: 150px;" title="@item.Reason">
                                            @item.Reason
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">Không có</span>
                                    }
                                </td>
                                <td>
                                    @if (item.Status == "Scheduled")
                                    {
                                        <span class="badge bg-warning">Đã đặt</span>
                                    }
                                    else if (item.Status == "Confirmed")
                                    {
                                        <span class="badge bg-info">Đã xác nhận</span>
                                    }
                                    else if (item.Status == "Completed")
                                    {
                                        <span class="badge bg-success">Hoàn thành</span>
                                    }
                                    else if (item.Status == "Cancelled")
                                    {
                                        <span class="badge bg-danger">Đã hủy</span>
                                    }
                                    else if (item.Status == "NoShow")
                                    {
                                        <span class="badge bg-secondary">Không đến</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">@item.Status</span>
                                    }
                                </td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">
                                            <i class="fas fa-eye"></i> Xem
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-warning">
                                            <i class="fas fa-edit"></i> Sửa
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">
                                            <i class="fas fa-trash"></i> Xóa
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-5">
                <i class="fas fa-calendar-alt fa-3x text-muted mb-3"></i>
                <h4 class="text-muted">Chưa có lịch hẹn nào</h4>
                <p class="text-muted">Hãy đặt lịch hẹn đầu tiên để bắt đầu sử dụng hệ thống.</p>
                <a asp-action="Create" class="btn btn-primary">
                    <i class="fas fa-plus"></i> Đặt lịch hẹn đầu tiên
                </a>
            </div>
        }
    </div>
</div>
